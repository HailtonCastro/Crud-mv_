spring:
    datasource:
        password: hito_bel
        url: jdbc:postgresql://localhost:5432/crud_mv
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
        show-sql: true
